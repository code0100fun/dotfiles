- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.bashrc: bashrc
    ~/.bash_aliases: bash_aliases

- shell:
  - [git submodule update --init --recursive, Installing submodules]
